import { Box } from "@mui/material"

const Header = () => {
  return (
    <Box
      sx={{
        background: {
          md: 'black',
          lg: 'red'
        }
      }}
    >Header</Box>
  )
}

export default Header
