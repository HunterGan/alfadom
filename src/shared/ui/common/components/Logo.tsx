import { Box } from "@mui/material"

const Logo = () => {
  return (
    <Box
      sx={{
        width: '100%',
        minHeight: 80,
        background: '#fff',
      }}
    >
      Alfa-Dom
    </Box>
  )
}

export default Logo
